---
story_key: 2-6-재고-조정-기록
story_id: "2.6"
epic: "2"
created_at: "2026-06-12T03:34:24+09:00"
source_story: "_bmad-output/planning-artifacts/epics.md"
baseline_commit: 36a8c66
---

# Story 2.6: 재고 조정 기록

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 지점장 또는 본사 사용자,
I want 시스템 재고와 실제 재고가 다를 때 조정 기록을 남길 수 있기를 원한다,
so that 재고 차이의 원인과 변경 근거를 나중에 확인할 수 있다.

## Acceptance Criteria

1. Given 사용자가 재고 행의 실제 수량이 시스템 계산값과 다르다고 확인했을 때, when 재고 조정을 추가한다, then 조정 전 수량, 조정 후 수량, 차이 수량, 사유를 기록해야 한다, and 작성자 표시명과 인증 actor를 구분해서 보존해야 한다.
2. Given 금액 또는 단가 근거가 필요한 조정 항목이 있을 때, when 정책이 아직 확정되지 않았다, then 조정 금액은 확정 계산으로 저장하지 않아야 한다, and 필요한 경우 `기준 확인 필요` 상태로 표시해야 한다.
3. Given 장부가 본사 마감 전일 때, when 권한 있는 지점장 또는 본사 사용자가 재고 조정을 저장한다, then 조정 기록은 해당 장부 재고 흐름에 반영되어야 한다, and 변경 전/후 값과 사유가 감사 로그에 남아야 한다.
4. Given 장부가 본사 마감 후일 때, when 사용자가 재고 원본 조정을 추가하려 한다, then 원본 수정은 차단되어야 한다, and 마감 후 변경은 정정 기록 흐름에서만 처리되어야 한다.
5. Given 사용자가 조정 사유 없이 저장하려 할 때, when 서버가 요청을 검증한다, then 저장은 거부되어야 한다, and UI는 사유 입력 오류를 해당 필드와 연결해 표시해야 한다.
6. Given 지점장 권한으로 조정 내역을 볼 때, when 서버가 응답 데이터를 만든다, then 원가나 재고금액을 역추정할 수 있는 민감 필드는 제외되어야 한다, and 수량, 상태, 사유 중심으로 표시되어야 한다.

## Tasks / Subtasks

- [x] 현재 구현된 재고 조정 모델/action/UI를 Story 2.6 기준으로 재검토한다. (AC: 1-6)
  - [x] 먼저 `prisma/schema.prisma`, `src/features/inventory/actions.ts`, `queries.ts`, `schemas.ts`, `types.ts`, `components/inventory-step-client.tsx`, `src/features/inventory/adjustment-reconciliation.ts`, `src/server/calculations/inventory.ts`, `tests/unit/ledger-inventory.test.mjs`, `tests/e2e/store-ledger-inventory-adjustment.spec.ts`를 읽는다.
  - [x] 이미 존재하는 `LedgerInventoryAdjustment`, `saveLedgerInventoryAdjustment`, `ledgerInventoryAdjustmentSchema`, `InventoryAdjustmentView`, `reconcileLedgerInventoryAdjustments`, `calculateSystemInventoryQuantity`를 삭제하거나 병렬 구현하지 않는다.
  - [x] `_bmad-output/story-automator/stale-story-artifacts/20260612-2-6/...`의 과거 2.6 손실 story는 stale artifact다. 현재 authoritative source는 `epics.md`의 `Story 2.6: 재고 조정 기록`과 `sprint-status.yaml`의 `2-6-재고-조정-기록`이다.
- [x] 조정 기록 persistence를 현재 AC와 정책 게이트에 맞게 정리한다. (AC: 1, 2, 3)
  - [x] 조정에는 품목 snapshot, 조정 전 수량, 조정 후 수량, 차이 수량, 사유, 인증 actor, 작성 시각/수정 시각을 반드시 남긴다.
  - [x] `authorDisplayName`은 장부 작성 흐름의 화면 표시명이고 감사 actor를 대체하지 않는다. UI/조회에서 표시가 필요하면 작성자 표시명과 인증 actor의 의미를 분리한다.
  - [x] 현재 `LedgerInventoryAdjustment`의 `beforeAmount`, `afterAmount`, `differenceAmount`, `unitPrice`는 PRD FR-10에는 있지만 Story 2.6 AC 2가 "정책 미확정 시 조정 금액 확정 계산 저장 금지"를 요구한다. OQ-7/OQ-17/FIFO 처리 순서와 가격 신뢰 정책이 닫히지 않았으므로 dev agent는 금액 필드를 확정 운영값으로 취급하거나 지점장에게 노출하면 안 된다.
  - [x] 스키마를 바꿔 금액 필드를 nullable/status화할지, 기존 not-null 필드를 유지하되 본사 내부 참고값 + `POLICY_UNCONFIRMED`/`기준 확인 필요`로 분리할지 결정한다. 기존 migration은 수정하지 말고 새 migration으로만 반영한다.
  - [x] 조정 수량이 0이면 저장하지 않는 현재 방어를 유지한다.
- [x] 재고 조정 저장 action을 권한, 상태, 충돌, 감사 기준에 맞게 강화한다. (AC: 3, 4, 5)
  - [x] `saveLedgerInventoryAdjustment`는 Zod validation, `requireStoreAccess`, DB transaction, version guard, audit write, revalidation을 유지한다.
  - [x] `IN_PROGRESS`와 `IN_REVIEW`는 원본 조정을 허용한다. `IN_REVIEW`는 본사 마감 전 상태이며 원본 잠금이 아니다.
  - [x] `HEADQUARTERS_CLOSED`와 `HOLIDAY`에서는 원본 조정을 막고 정정 기록 흐름을 안내한다. 마감 후 원본값을 update/upsert하면 안 된다.
  - [x] stale `version`은 `LEDGER_CONFLICT`로 반환하고, 조용한 마지막 저장 덮어쓰기를 허용하지 않는다.
  - [x] 사유는 서버에서 필수 검증한다. client-only required 처리에 의존하지 않는다.
  - [x] 감사 로그 action은 `ledger.inventory_adjustment.saved` 또는 현재 저장소의 기존 이벤트명을 유지하고, `before`, `after`, `reason`, `actorId`, target ledger를 기록한다.
- [x] 재고 흐름 반영과 기존 조정 재계산을 보존한다. (AC: 1, 3)
  - [x] 조정 전 시스템 수량은 `previousQuantity + purchasedQuantity - lossQuantity` 기준으로 계산한다.
  - [x] 조정 후 실제 수량은 `LedgerInventoryItem.currentQuantity`에 반영한다. 별도 `quantity` 필드를 임의로 덮어쓰지 않는다.
  - [x] 매입/손실/재고 저장 뒤 기존 조정 기준이 stale이 되지 않도록 `reconcileLedgerInventoryAdjustments` 경로를 유지하고, 손실 변경 후에도 before/difference가 최신 기준인지 확인한다.
  - [x] 과다 손실 또는 데이터 부족으로 시스템 수량이 계산 불가면 조정을 저장하지 말고 `계산 불가`/`기준 확인 필요` 계열 메시지를 반환한다. 0으로 대체하지 않는다.
- [x] 지점장 응답과 UI에서 민감 금액/원가 노출을 차단한다. (AC: 2, 6)
  - [x] 지점장 경로는 반드시 `toStoreManagerInventoryStepData` 또는 동등한 server-side safe mapper를 거친다.
  - [x] 지점장 응답/HTML/serialized payload에 `unitPrice`, `purchaseAmount`, `lossAmount`, `inventoryAmount`, `beforeAmount`, `afterAmount`, `differenceAmount`, FIFO/lot 근거, 원가/마진 파생값이 포함되지 않게 유지한다.
  - [x] 본사/HQ 경로에서 금액을 표시하더라도 MVP 기본 계산값, FIFO 기준 계산값, `기준 확인 필요` 상태를 섞지 않는다.
  - [x] `InventoryStepClient`가 지점장과 본사 데이터를 공유할 때 client-only hide가 보안 경계가 되지 않게 한다.
- [x] 재고 조정 UI를 현장 입력 흐름에 맞게 정리한다. (AC: 1, 4, 5, 6)
  - [x] 기존 재고 테이블의 냉동/생물 탭, sticky header, 50행 paging, `LedgerSaveStatus`, `UnsavedChangeDialog`, 하단 sticky 저장 버튼, KST date query 보존을 깨지 않는다.
  - [x] 실제 재고 수량이 시스템 계산 수량과 다를 때만 `조정 필요`를 표시하고, 저장된 행은 `조정됨`과 조정 전/후/차이 수량, 사유를 수량 중심으로 보여준다.
  - [x] 조정 사유 오류는 해당 행의 사유 필드와 `aria-describedby`로 연결하고, 제출 실패 시 해당 필드로 포커스를 이동한다.
  - [x] 조정 저장 중 다른 행의 조정 버튼을 잠그거나 version conflict를 명확히 처리해 응답 순서로 stale 상태가 보이지 않게 한다.
  - [x] 본사 마감 장부에서는 원본 조정 입력과 저장 UI를 비활성화하고 정정 기록 안내를 표시한다.
- [ ] 테스트를 Story 2.6 기준으로 추가/갱신한다. (AC: 1-6)
  - [x] `tests/unit/ledger-inventory.test.mjs`에서 조정 모델/action/schema/audit/safe mapper 계약을 현재 Story 2.6 명명으로 갱신한다.
  - [x] 금액 정책 게이트를 테스트한다. 지점장 safe payload에는 조정 금액 필드가 없어야 하며, 금액이 노출되는 본사 경로가 있으면 `기준 확인 필요` 상태와 혼동되지 않아야 한다.
  - [x] `tests/e2e/store-ledger-inventory-adjustment.spec.ts`의 marker/seed 이름을 `story-2-6`으로 정리하고, 실제 재고 차이 저장, 사유 필수 오류, 재방문 표시, 감사 로그 reason, 본사 마감 후 차단, 민감 금액 미노출을 검증한다.
  - [x] 손실 저장/수정이 기존 조정 before/difference를 재계산하는 회귀를 포함한다.
  - [ ] focused verification: `corepack pnpm exec node --experimental-strip-types --test tests/unit/ledger-inventory.test.mjs`와 `PORT=31xx DATABASE_URL=postgresql://postgres:erp_fish_local_pw@host.docker.internal:5432/erp_fish_e2e corepack pnpm exec playwright test tests/e2e/store-ledger-inventory-adjustment.spec.ts`.

## Dev Notes

### Source Context

- Epic 2는 지점장 또는 본사가 하루 장부를 만들고 매출/비용/매입/재고/손실/근무 정보를 모바일 우선 7단계 흐름으로 입력하는 영역이다. [Source: `_bmad-output/planning-artifacts/epics.md#Epic 2: 지점 일일 장부 입력`]
- Story 2.6은 시스템 계산 재고와 실제 재고가 다를 때 재고 조정을 기록하고, 마감 전 재고 흐름과 감사 이력에 반영하며, 마감 후에는 정정 기록으로만 처리하도록 요구한다. [Source: `_bmad-output/planning-artifacts/epics.md#Story 2.6: 재고 조정 기록`]
- PRD FR-10은 조정 전/후 수량과 금액, 차이 수량/금액, 사유, 작성자, 작성 시각을 요구하지만, 현재 story AC 2는 정책 미확정 시 조정 금액 확정 계산 저장 금지를 추가로 요구한다. Story 구현은 둘을 조화시켜 금액 필드를 확정 운영값처럼 쓰지 않게 해야 한다. [Source: `_bmad-output/planning-artifacts/prds/prd-erp_fish-2026-05-28-2/prd.md#FR-10: 재고 조정 기록`]
- PRD FR-9 재고 흐름은 시스템 계산 재고와 실제 입력 재고의 차이 확인, 직전 저장 장부/본사 마감 기준, 이월 상태를 정의한다. 조정 전 수량 계산은 이 흐름과 맞아야 한다. [Source: `_bmad-output/planning-artifacts/prds/prd-erp_fish-2026-05-28-2/prd.md#FR-9: 품목 단위 재고 입력`]
- PRD 계산 공통 규칙은 FIFO lot, 반품/조정/폐기/떨이 처리 순서, 가격 기준이 확정되기 전에는 확정 원가/재고금액처럼 표시하지 말고 `기준 확인 필요` 상태를 구분하라고 요구한다. [Source: `_bmad-output/planning-artifacts/prds/prd-erp_fish-2026-05-28-2/prd.md#4.3 계산과 검증`]
- UX는 재고 테이블에서 품목, 규격, 전일재고 후보, 확정 이월 기준, 매입, 손실, 당일재고, 상태 컬럼을 요구하고, 지점장에게 재고금액/원가 근거를 숨기라고 정의한다. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-erp_fish-2026-05-28/EXPERIENCE.md#Component Patterns`]

### Current Repository State

- Sprint status의 authoritative key는 `2-6-재고-조정-기록`이며 생성 전 상태는 `backlog`였다. [Source: `_bmad-output/implementation-artifacts/sprint-status.yaml`]
- 기존 `2-6-지점장이-손실-폐기-떨이를-입력하고-재고-흐름에-반영한다.md`는 current epics와 sprint status에서 Story 2.7에 해당하는 과거 산출물이다. 혼동 방지를 위해 `_bmad-output/story-automator/stale-story-artifacts/20260612-2-6/`로 이동했다.
- 현재 `prisma/schema.prisma`에는 이미 `LedgerInventoryAdjustment`가 있다. 필드는 `unitPrice`, `beforeQuantity`, `beforeAmount`, `afterQuantity`, `afterAmount`, `differenceQuantity`, `differenceAmount`, `reason`, `createdById`, `updatedById`를 포함한다. [Source: `prisma/schema.prisma`]
- 현재 `saveLedgerInventoryAdjustment`는 `requireStoreAccess`, transaction, version guard, `IN_PROGRESS`/`IN_REVIEW` 허용, `HEADQUARTERS_CLOSED` 차단, `ledger.inventory_adjustment.saved` audit, revalidation을 이미 갖고 있다. [Source: `src/features/inventory/actions.ts`]
- 현재 조정 계산은 `calculateInventoryAdjustment`가 금액까지 계산한다. Story 2.6 AC 2와 PRD 가격/FIFO 게이트 때문에 dev agent는 이 금액을 지점장 surface나 확정 운영 지표로 쓰면 안 된다. [Source: `src/server/calculations/inventory.ts`]
- 현재 `toStoreManagerInventoryStepData`는 지점장 응답에서 `unitPrice`, `purchaseAmount`, `lossAmount`, `inventoryAmount`, 조정 금액 필드를 제거한다. 이 보안 계약은 반드시 유지한다. [Source: `src/features/inventory/queries.ts`]
- 현재 `tests/e2e/store-ledger-inventory-adjustment.spec.ts`는 marker가 `story-2-5-test`로 남아 있고 조정 흐름을 검증한다. Story 2.6 구현 중 테스트 명명과 범위를 현재 story로 정리한다. [Source: `tests/e2e/store-ledger-inventory-adjustment.spec.ts`]

### Architecture Guardrails

- 선택 스택은 Create T3 App 기반 Next.js App Router, TypeScript, Prisma, PostgreSQL, NextAuth/Auth.js, Tailwind CSS, shadcn/ui, Vercel이다. Story 2.6에서 ORM, auth framework, UI primitive 체계를 바꾸지 않는다. [Source: `_bmad-output/planning-artifacts/architecture.md#Starter Template Evaluation`]
- Prisma schema and migrations are the source of truth for persisted data. schema 변경은 새 migration으로 반영하고 기존 migration을 수정하지 않는다. [Source: `_bmad-output/planning-artifacts/architecture.md#Data Architecture`]
- 대부분의 제품 동작은 Server Components와 Server Actions를 사용한다. 재고 조정 저장을 위해 public API route를 만들지 않는다. [Source: `_bmad-output/planning-artifacts/architecture.md#API & Communication Patterns`]
- 모든 mutation은 Zod validation, shared authorization helper, transaction, audit, revalidation을 따라야 한다. [Source: `_bmad-output/planning-artifacts/architecture.md#Process Patterns`]
- Store manager responses must omit sensitive accounting fields. Story 2.6은 수량/상태/사유 중심이며, 지점장 응답에 재고금액, 원가, lot 근거, 마진/이익 파생값을 보내면 안 된다. [Source: `_bmad-output/planning-artifacts/architecture.md#Authorization Pattern`]
- `확인 필요`, `계산 불가`, `데이터 부족`은 shared status enum 또는 display helper로 맞춰야 하며 각 화면이 다른 라벨을 임의로 만들면 안 된다. [Source: `_bmad-output/planning-artifacts/architecture.md#Error Handling Pattern`]

### Previous Story Intelligence

- Story 2.1의 `version` guard와 `LEDGER_CONFLICT` message를 유지한다. 조정 저장은 stale form 값을 조용히 덮어쓰면 안 된다. [Source: `_bmad-output/implementation-artifacts/2-1-지점-일자-장부-생성과-상태-관리.md`]
- Story 2.2의 `LedgerSaveStatus`, `UnsavedChangeDialog`, `useUnsavedStepGuard`가 단계별 저장 안정성 표면이다. 조정 UI에서 별도 modal/status 패턴을 새로 만들지 않는다. [Source: `_bmad-output/implementation-artifacts/2-2-장부-입력-7단계-폼과-작성자-표시명-유지.md`]
- Story 2.3은 `IN_PROGRESS`와 `IN_REVIEW` 원본 수정 허용, `HEADQUARTERS_CLOSED`/`HOLIDAY` 차단, server validation, audit, revalidation, KST date 보존을 강화했다. 재고 조정도 같은 원칙을 따른다. [Source: `_bmad-output/implementation-artifacts/2-3-매출-결제와-비용-입력.md`]
- Story 2.4는 수동 매입 입력을 완료했다. 조정 전 시스템 수량은 매입 집계와 손실 집계를 함께 반영해야 한다. [Source: `_bmad-output/implementation-artifacts/2-4-mvp-수동-매입-입력.md`]
- Story 2.5는 `InventoryCarryoverStatus`, `PREVIOUS_SAVED_LEDGER`, 50행 paging, 지점장 safe response shaping을 완료했다. 조정 구현은 이 재고 step을 재작성하지 말고 기존 경로를 보강한다. [Source: `_bmad-output/implementation-artifacts/2-5-재고-입력과-이월-상태-표시.md`]
- Recent git history includes `36a8c66 feat(story-2.5): 재고 이월 상태 입력 보강`, `a7fb571 feat(story-2.4): 수동 매입 입력 지원`, `70628d8 feat(story-2.3): 매출 결제 비용 입력 보강`. Current implementation should be read from files, but these commits confirm story order. [Source: `git log --oneline -8`]

### File Structure Requirements

- 예상 UPDATE 파일:
  - `prisma/schema.prisma`
  - `prisma/migrations/*_inventory_adjustment_policy*/migration.sql` if 금액/status persistence changes are needed
  - `src/features/inventory/actions.ts`
  - `src/features/inventory/queries.ts`
  - `src/features/inventory/schemas.ts`
  - `src/features/inventory/types.ts`
  - `src/features/inventory/components/inventory-step-client.tsx`
  - `src/features/inventory/adjustment-reconciliation.ts`
  - `src/server/calculations/inventory.ts`
  - `tests/unit/ledger-inventory.test.mjs`
  - `tests/e2e/store-ledger-inventory-adjustment.spec.ts`
  - `tests/e2e/store-ledger-losses.spec.ts` only if 손실 변경 후 조정 재계산 regression is added there.
- 예상 NEW 파일:
  - Optional: `src/features/inventory/adjustment-policy.ts` if 조정 금액/정책 상태 분리가 여러 파일에서 필요하다.
  - Optional: focused unit test file if `ledger-inventory.test.mjs` becomes too broad.
- 금지:
  - 신규 public API route for inventory adjustments.
  - 기존 `LedgerInventoryAdjustment`와 병렬인 별도 조정 persistence table without a documented migration reason.
  - FIFO lot/remaining quantity calculation or `PurchaseLot` implementation.
  - `30%단가` 파생 표시.
  - 지점장 민감 회계 지표, 원가, 재고금액, lot 근거 노출.
  - 기존 migration 수정.
  - unrelated dirty worktree revert.

### Testing Requirements

- Run focused unit tests:
  - `corepack pnpm exec node --experimental-strip-types --test tests/unit/ledger-inventory.test.mjs`
  - any new focused inventory adjustment policy unit test.
- Run focused E2E:
  - `PORT=31xx DATABASE_URL=postgresql://postgres:erp_fish_local_pw@host.docker.internal:5432/erp_fish_e2e corepack pnpm exec playwright test tests/e2e/store-ledger-inventory-adjustment.spec.ts`
- Run related regression E2E if loss or inventory reconciliation changes:
  - `PORT=31xx DATABASE_URL=postgresql://postgres:erp_fish_local_pw@host.docker.internal:5432/erp_fish_e2e corepack pnpm exec playwright test tests/e2e/store-ledger-inventory.spec.ts tests/e2e/store-ledger-losses.spec.ts`
- Run full verification before moving to review:
  - `corepack pnpm exec prisma generate`
  - `corepack pnpm exec prisma validate`
  - `corepack pnpm lint`
  - `corepack pnpm typecheck`
  - `corepack pnpm test:unit`
  - `corepack pnpm build`
- If the local database or Playwright browser dependencies are unavailable, record the skipped command and exact blocker in Dev Agent Record.

### Latest Technical Information

- Package versions in this workspace: Next.js `^15.2.3`, React `^19.0.0`, Prisma `^6.6.0`, NextAuth `5.0.0-beta.25`, Tailwind CSS `^4.0.15`, shadcn `^4.8.2`, Playwright `^1.60.0`, Zod `^3.24.2`. [Source: `package.json`]
- Story 2.6 introduces no new external API or library. Network research was not performed in this create-story run; use pinned workspace versions and established Story 2.1-2.5 implementation patterns unless a dev agent explicitly upgrades dependencies.

### Project Context Reference

- Workflow persistent facts requested `file:{project-root}/**/project-context.md`, but no `project-context.md` file was found in the repository.
- Discovery loaded: `epics.md`, `architecture.md`, PRD shard `prds/prd-erp_fish-2026-05-28-2/prd.md`, UX `DESIGN.md`/`EXPERIENCE.md`, Story 2.5, current inventory/ledger code, inventory unit/e2e tests, sprint status, git status/history, and package versions.

### Implementation Boundaries

- 포함: 현재 조정 모델/action/UI 재사용, 조정 전/후/차이 수량 기록, 사유 필수, 인증 actor/audit reason 기록, 마감 전 재고 흐름 반영, 마감 후 원본 조정 차단, 지점장 민감 금액 차단, 금액/단가 정책 미확정 상태 표시, version conflict guard, 관련 테스트 정리.
- 제외: FIFO lot/원가/잔량, `30%단가`, 확정 재고금액, 본사 통합 재고, 상품별 관리자 분석, 월 손익, CAP-13 민감 지표 노출 정책 고도화, 마감 후 정정 반영값 확정 계산, 재고 조정과 별개인 손실 입력 신규 구현.
- 이 story는 "재고 조정을 처음부터 새로 만드는" 작업이 아니다. 현재 구현된 adjustment path를 current Story 2.6 AC와 PRD 게이트에 맞게 검증하고 gap을 보강한다.

### Validation Notes

- Checklist 재분석 결과 핵심 위험은 7가지다.
- Critical 1: 기존 조정 action은 금액을 계산해 저장한다. Story 2.6 AC 2와 OQ-7/OQ-17이 닫히지 않은 상태에서 이 금액을 확정 운영값으로 쓰면 정책 위반이다.
- Critical 2: 기존 E2E/단위 테스트 명명이 Story 2.5로 남아 있다. Dev agent가 current sprint 기준을 혼동하지 않도록 Story 2.6 명명으로 정리해야 한다.
- Critical 3: 지점장 응답에서 조정 금액 필드가 새 경로를 통해 재노출되면 Story 1.4 보안 계약과 AC 6이 깨진다.
- Critical 4: 매입/손실 변경 후 기존 조정의 before/difference가 stale이면 재고 흐름과 리포트가 어긋난다.
- Critical 5: 조정 저장 중 다른 행 조정이나 미저장 재고 입력을 덮으면 Story 2.1/2.2의 충돌·미저장 변경 방어가 깨진다.
- Critical 6: 본사 마감 후 원본 조정 upsert가 가능하면 NFR2와 마감/정정 정책을 위반한다.
- Critical 7: 작성자 표시명과 인증 actor를 섞으면 화면 작성자와 감사 책임자가 혼동된다.

## Project Structure Notes

- Architecture target tree는 `src/features/inventory`, `src/features/ledger`, `src/server/authz.ts`, `src/server/audit.ts`, `src/server/calculations`, `prisma`, `tests/unit`, `tests/e2e` 경계를 사용한다.
- `src/components/ui`에는 shadcn primitive만 둔다. 재고 step-specific UI는 `src/features/inventory/components`에 둔다.
- `src/server` owns cross-cutting auth/authz/audit/db behavior. Feature actions call helpers; they do not copy auth/session/audit logic.
- 현재 implementation artifacts에는 과거 Story 2.6 손실 파일이 stale 폴더로 이동되어 있다. 새 authoritative story 파일은 이 파일이다.

### References

- [Source: `_bmad-output/planning-artifacts/epics.md#Story 2.6: 재고 조정 기록`]
- [Source: `_bmad-output/planning-artifacts/prds/prd-erp_fish-2026-05-28-2/prd.md#FR-10: 재고 조정 기록`]
- [Source: `_bmad-output/planning-artifacts/prds/prd-erp_fish-2026-05-28-2/prd.md#4.3 계산과 검증`]
- [Source: `_bmad-output/planning-artifacts/architecture.md#Data Architecture`]
- [Source: `_bmad-output/planning-artifacts/architecture.md#API & Communication Patterns`]
- [Source: `_bmad-output/planning-artifacts/architecture.md#Authorization Pattern`]
- [Source: `_bmad-output/planning-artifacts/ux-designs/ux-erp_fish-2026-05-28/EXPERIENCE.md#Component Patterns`]
- [Source: `prisma/schema.prisma`]
- [Source: `src/features/inventory/actions.ts`]
- [Source: `src/features/inventory/queries.ts`]
- [Source: `src/features/inventory/components/inventory-step-client.tsx`]
- [Source: `src/features/inventory/types.ts`]
- [Source: `src/features/inventory/adjustment-reconciliation.ts`]
- [Source: `src/server/calculations/inventory.ts`]
- [Source: `tests/unit/ledger-inventory.test.mjs`]
- [Source: `tests/e2e/store-ledger-inventory-adjustment.spec.ts`]

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-06-12T03:44:56+09:00 `corepack pnpm exec prisma generate` 통과.
- 2026-06-12T03:44:56+09:00 `corepack pnpm exec prisma validate` 통과.
- 2026-06-12T03:44:56+09:00 `corepack pnpm exec node --experimental-strip-types --test tests/unit/ledger-inventory.test.mjs` 통과.
- 2026-06-12T03:44:56+09:00 `corepack pnpm typecheck` 통과.
- 2026-06-12T03:44:56+09:00 `corepack pnpm lint` 통과.
- 2026-06-12T03:44:56+09:00 `corepack pnpm test:unit` 통과: 29/29.
- 2026-06-12T03:44:56+09:00 `corepack pnpm build` 통과.
- 2026-06-12T03:44:56+09:00 `PORT=3106 DATABASE_URL=postgresql://postgres:erp_fish_local_pw@host.docker.internal:5432/erp_fish_e2e corepack pnpm exec playwright test tests/e2e/store-ledger-inventory-adjustment.spec.ts` 실패: Playwright webServer 조기 종료.
- 2026-06-12T03:44:56+09:00 직접 서버 실행 확인: `listen EPERM: operation not permitted 127.0.0.1:3106`. 현재 sandbox가 로컬 포트 listen을 막아 E2E 실행 불가.
- 2026-06-12T03:57:17+09:00 Orchestrator final verification on PORT=3100 passed focused adjustment E2E 4/4; typecheck, lint, full unit 196/196, and build passed.
- 2026-06-12T04:04:12+09:00 Automate guardrail added loss-to-adjustment recalculation E2E; orchestrator verification on PORT=3100 passed focused adjustment E2E 5/5.
- 2026-06-12T04:10:21+09:00 Senior review auto-fix: store-manager adjustment validation now completes before ledger version mutation, and full draft save is blocked while an adjustment save is pending.
- 2026-06-12T04:10:21+09:00 Senior review verification passed: `corepack pnpm exec prisma validate`, `corepack pnpm exec node --experimental-strip-types --test tests/unit/ledger-inventory.test.mjs`, `corepack pnpm typecheck`, `corepack pnpm lint`, `corepack pnpm test:unit`, `corepack pnpm build`.
- 2026-06-12T04:10:21+09:00 Senior review focused E2E attempt in child session was blocked by sandbox port restriction: direct Next server failed with `listen EPERM: operation not permitted 127.0.0.1:3100`.
- 2026-06-12T04:14:54+09:00 Orchestrator final verification after senior review auto-fixes passed: `corepack pnpm exec prisma validate`, focused unit 13/13, `corepack pnpm typecheck`, `corepack pnpm lint`, focused E2E 5/5 on PORT=3100, full unit 197/197, and `corepack pnpm build`.

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created
- 기존 `LedgerInventoryAdjustment` 경로를 유지하면서 `InventoryAdjustmentAmountStatus`와 새 migration을 추가해 조정 금액을 `POLICY_UNCONFIRMED` 상태로 분리했다.
- 지점장 safe mapper와 UI는 수량, 상태, 사유 중심으로 조정 내역을 표시하고 조정 금액/원가 파생값을 노출하지 않는다.
- `HEADQUARTERS_CLOSED`와 `HOLIDAY` 원본 조정 차단, 계산 불가 기준 메시지, 조정 재계산 정책 상태 갱신을 보강했다.
- Story 2.6 기준 unit/E2E 테스트 계약을 갱신했다. child session에서는 PORT=3106 bind 제한으로 막혔지만, 최종 오케스트레이터 검증은 PORT=3100에서 focused adjustment E2E 5/5를 통과했다.
- Senior review에서 실패한 조정 요청이 ledger version을 증가시키던 부작용과 조정 저장 중 전체 재고 저장이 가능하던 UI 동시성 gap을 자동 수정했다.

### File List

- `_bmad-output/implementation-artifacts/2-6-재고-조정-기록.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `prisma/schema.prisma`
- `prisma/migrations/20260612034000_inventory_adjustment_amount_status/migration.sql`
- `src/features/inventory/actions.ts`
- `src/features/inventory/adjustment-reconciliation.ts`
- `src/features/inventory/components/inventory-step-client.tsx`
- `src/features/inventory/hq-edit-actions.ts`
- `src/features/inventory/queries.ts`
- `src/features/inventory/types.ts`
- `tests/e2e/store-ledger-inventory-adjustment.spec.ts`
- `tests/unit/ledger-inventory.test.mjs`

### Senior Developer Review (AI)

Reviewer: GPT-5 Codex
Review Date: 2026-06-12
Outcome: Approved after auto-fix

Findings fixed:

- HIGH: `saveLedgerInventoryAdjustment`가 품목/기준수량/0차이 validation 전에 `dailyLedger.updateMany`로 version을 증가시켜, 거부된 요청도 장부 변경으로 남을 수 있었다. Validation을 모두 통과한 뒤에만 version guard mutation을 수행하도록 수정했다. [Source: `src/features/inventory/actions.ts`]
- MEDIUM: 조정 저장 중 행 입력과 조정 버튼은 잠겼지만 하단 전체 저장 버튼과 draft save 경로가 열려 있어 같은 ledger version으로 병렬 저장이 가능했다. `isAdjustmentSavePending` 동안 전체 저장을 차단하고 사용자 메시지와 test guard를 추가했다. [Source: `src/features/inventory/components/inventory-step-client.tsx`, `tests/unit/ledger-inventory.test.mjs`]
- LOW: `HOLIDAY` 장부에서 재고 원본 수정 차단 alert가 본사 마감 장부 문구를 표시했다. 상태별 alert title/message를 분리했다. [Source: `src/features/inventory/components/inventory-step-client.tsx`]

Validation:

- Passed: `corepack pnpm exec prisma validate`
- Passed: `corepack pnpm exec node --experimental-strip-types --test tests/unit/ledger-inventory.test.mjs`
- Passed: `corepack pnpm typecheck`
- Passed: `corepack pnpm lint`
- Passed: `corepack pnpm test:unit`
- Passed: `corepack pnpm build`
- Passed in orchestrator final verification: `PORT=3100 DATABASE_URL=postgresql://postgres:erp_fish_local_pw@host.docker.internal:5432/erp_fish_e2e corepack pnpm exec playwright test tests/e2e/store-ledger-inventory-adjustment.spec.ts` - 5/5.

Checklist result:

- Story status was reviewable before auto-fix.
- Acceptance Criteria 1-6 cross-checked against implementation.
- File List and git changes reviewed; unrelated dirty planning/agent artifacts were not reverted.
- Tests mapped to ACs; new unit guard added for rejected adjustment version mutation and adjustment-save concurrency.
- Code quality, security, sensitive-field exposure, status locking, audit reason, and reconciliation paths reviewed.
- Story status set to done; sprint status synced.

### Change Log

- 2026-06-12: Story 2.6 재고 조정 금액 정책 상태, 마감/휴무 차단, safe payload/UI 노출 정책, 테스트 계약을 보강했다. 최종 오케스트레이터 검증에서 focused E2E 5/5를 통과했다.
- 2026-06-12: Senior review auto-fix로 rejected adjustment version mutation, 조정 저장 중 전체 저장 동시성, 휴무 장부 안내 문구를 수정하고 focused/unit/type/lint/build 검증을 통과했다.
