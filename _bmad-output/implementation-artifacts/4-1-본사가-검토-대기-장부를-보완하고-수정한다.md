---
story_key: 4-1-본사가-검토-대기-장부를-보완하고-수정한다
story_id: "4.1"
epic: "4"
created_at: "2026-06-01T10:20:51+09:00"
source_story: "C:\\Code\\Project\\erp_fish\\_bmad-output\\planning-artifacts\\epics.md#Story-4.1"
baseline_commit: b1fc916
---

# Story 4.1: 본사가 검토 대기 장부를 보완하고 수정한다

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 본사 사용자,
I want 검토 대기 또는 입력중 장부를 직접 보완하고 수정하고 싶다,
so that 지점 데이터가 누락되거나 잘못되어도 본사가 회의 전에 장부를 정리할 수 있다.

## Acceptance Criteria

1. **Given** 본사 사용자가 입력중 또는 검토대기 장부 상세에 접근할 때
   **When** 장부 상세가 열리면
   **Then** 본사는 매출/결제, 비용, 매입, 재고, 재고 조정, 손실, 근무 정보를 조회하고 수정할 수 있어야 한다
   **And** 서버는 본사 권한을 확인해야 한다.

2. **Given** 본사 사용자가 지점장 입력값을 보완하거나 수정할 때
   **When** 변경 내용을 저장하면
   **Then** 장부 값이 갱신되어야 한다
   **And** 수정 주체는 본사 사용자로 기록되어야 한다.

3. **Given** 본사 수정이 저장될 때
   **When** 저장이 완료되면
   **Then** actor, timestamp, target, before, after를 포함하는 감사 기록이 남아야 한다
   **And** 지점장 입력/수정 이력과 본사 입력/수정 이력이 구분되어야 한다.

4. **Given** 본사 수정 후 계산값에 영향이 있을 때
   **When** 장부 상세 또는 관제판이 다시 조회되면
   **Then** shared server calculation을 사용한 계산값과 이상 신호가 갱신되어야 한다.

5. **Given** 본사 사용자가 권한 없는 경로나 잘못된 장부에 접근할 때
   **When** 서버가 요청을 처리하면
   **Then** 장부 데이터가 반환되지 않아야 한다
   **And** "접근 권한이 없습니다." 또는 찾을 수 없다는 한국어 안내가 표시되어야 한다.

6. **Given** 장부가 이미 본사마감 상태일 때
   **When** 본사 사용자가 원본 입력 필드를 수정하려 하면
   **Then** 원본 수정은 허용되지 않아야 한다
   **And** 정정 기록을 추가해야 한다는 안내가 표시되어야 한다.

7. **Given** 본사 수정 폼에 잘못된 값이 입력될 때
   **When** 저장을 시도하면
   **Then** 필드 하단 또는 행 안에 한국어 검증 메시지가 표시되어야 한다
   **And** 첫 번째 오류 위치로 포커스가 이동해야 한다.

## Tasks / Subtasks

- [x] 본사 장부 상세를 편집 가능한 상세 화면으로 확장한다. (AC: 1, 5, 6)
  - [x] `src/app/app/ledgers/[ledgerId]/page.tsx`의 현재 read-only 상세를 먼저 읽고, `ledgerId` 기준의 편집 섹션을 추가한다.
  - [x] 데스크탑은 현재 상세 페이지 흐름을 유지해도 된다. Sheet 전환은 필수가 아니다. 단, 관제판에서 전달된 `date`, `sort`, `filter` 복귀 상태는 잃으면 안 된다.
  - [x] 상세 헤더에는 장부 상태, 영업일, 지점명, 이상 신호 요약, 마지막 수정자/수정 시각을 유지한다.
  - [x] 상태가 `IN_PROGRESS` 또는 `IN_REVIEW`이면 원본 입력 섹션을 수정 가능하게 한다.
  - [x] 상태가 `HEADQUARTERS_CLOSED`이면 원본 입력 필드는 disabled 처리하고, "본사 마감된 장부입니다. 정정 기록을 추가해야 합니다."와 같은 안내를 표시한다. 정정 기록 작성 자체는 Story 4.3 범위다.
  - [x] 잘못된 `ledgerId`는 `notFound()` 또는 기존 권한 없음 화면 패턴으로 처리하고 데이터를 노출하지 않는다.

- [x] `ledgerId` 기준 읽기/쓰기 경계를 만든다. (AC: 1, 2, 4, 5, 6)
  - [x] 현재 `getTodayStoreLedger`, `getInventoryStepData`, `getLossStepData`와 기존 save actions는 대부분 `storeId -> 오늘 장부` 기준이다. 본사 상세 편집에서는 이 함수를 그대로 호출해 선택 장부가 아닌 오늘 장부를 수정하면 안 된다.
  - [x] `src/features/ledger/queries.ts` 또는 새 feature-local query에 `ledgerId`로 기존 장부를 조회하는 서버 query를 추가한다. 새 query는 장부를 생성하지 않아야 한다.
  - [x] 본사 수정 action 입력에는 `ledgerId`를 포함한다. `storeId`만 받는 기존 입력 action을 본사 상세에서 직접 쓰지 않는다.
  - [x] 서버 action은 `requireHeadquartersUser()` 또는 `requireStoreAccess(ledger.storeId)`와 명시적 role 확인으로 본사 권한을 강제한다. 클라이언트 버튼 숨김은 보안 경계가 아니다.
  - [x] 조건부 update는 `id: ledgerId`와 `status in ["IN_PROGRESS", "IN_REVIEW"]`를 함께 확인한다. `HEADQUARTERS_CLOSED`와 `HOLIDAY`는 원본 수정 금지다.
  - [x] `submittedById`와 `submittedAt`은 본사 보완 저장으로 바꾸지 않는다. 검토 대기 제출 이력은 Story 2.8의 제출 action만 소유한다.

- [x] 매출/결제, 비용, 매입, 근무 보완 저장을 연결한다. (AC: 1, 2, 3, 4, 7)
  - [x] `ledgerSalesPaymentSchema`, `ledgerExpenseSchema`, `ledgerPurchaseSchema`, `ledgerWorkInfoSchema`의 검증 규칙과 한국어 오류 메시지를 재사용한다.
  - [x] 기존 `SalesPaymentStepClient`, `ExpenseStepClient`, `PurchaseStepClient`, `WorkStepClient`를 재사용할 경우 `ledgerId` 기반 action을 호출할 수 있게 작게 확장한다.
  - [x] 기존 store-entry 지점장 UX를 깨지 않는다. 지점장용 today flow와 본사용 ledger detail flow가 같은 컴포넌트를 공유하더라도 서로 다른 저장 대상이 섞이면 안 된다.
  - [x] 저장 성공 후 `updatedById`는 본사 사용자 id로 갱신한다.
  - [x] 감사 action 이름은 역할 구분이 가능하게 정한다. 예: `ledger.hq.sales_payment.updated`, `ledger.hq.expenses.saved`, `ledger.hq.purchases.saved`, `ledger.hq.work_info.saved`.
  - [x] 비용/매입 저장이 전체 delete/create 방식을 유지한다면 before/after audit payload에 행 단위 정보가 충분히 남는지 확인한다. 행 identity와 `createdBy/createdAt` 보존이 필요한 경우 update/upsert/delete 차이를 분리한다.

- [x] 재고, 재고 조정, 손실 보완 저장을 연결한다. (AC: 1, 2, 3, 4, 7)
  - [x] `src/features/inventory/actions.ts`와 `src/features/losses/actions.ts`의 현재 `IN_PROGRESS`/`IN_REVIEW` 허용, closed-blocking, 한국어 오류 패턴을 재사용한다.
  - [x] 재고 조회/저장은 `ledgerId` 기준이어야 하며, 화면 표시용 carryover 행을 저장된 입력처럼 취급하면 안 된다.
  - [x] `quantity`와 `currentQuantity`의 의미를 섞지 않는다. 시스템 수량, 실제 입력 수량, 조정 수량은 기존 Story 2.4/2.5 규칙을 유지한다.
  - [x] 재고 조정은 조정 사유 필수와 첫 오류 포커스 이동을 유지한다.
  - [x] 손실 저장은 품목, 손실 유형, 수량/금액, 사유 검증을 유지한다.
  - [x] 손실/재고 저장 후 `reconcileLedgerInventoryAdjustments`와 이상 신호 계산에 필요한 기존 흐름을 깨지 않는다.

- [x] 감사 이력에서 본사 수정과 지점장 수정을 구분한다. (AC: 2, 3)
  - [x] 모든 본사 보완 저장은 `writeAuditLog`를 비즈니스 변경과 같은 transaction 안에서 호출한다.
  - [x] audit payload에는 actor, target, before, after가 들어가야 하며, timestamp는 `AuditLog.createdAt`으로 남는다.
  - [x] `src/features/audit/audit-format.ts`에 본사 수정 action label을 추가한다. 예: "본사 매출/결제 수정", "본사 매입 수정".
  - [x] `src/features/audit/audit-queries.ts`의 `DailyLedger` 대상 이름 해석은 기존 Story 2.8 패턴을 유지한다.
  - [x] 감사 UI에서 actor 이름만으로 역할이 모호하면 action label에 "본사"를 포함한다. schema에 role snapshot을 추가하는 것은 이번 story의 필수 요구가 아니다.

- [x] 저장 후 상세, 관제판, 검토 화면이 낡은 값을 보지 않게 갱신한다. (AC: 4)
  - [x] 저장 성공 후 `/app/ledgers/[ledgerId]`, `/app/dashboard`, 관련 `/app/store-entry*` 경로를 revalidate한다.
  - [x] `getHqLedgerDetail()` 또는 새 상세 query는 저장 후 shared calculation 결과와 `DashboardSignalSummary`에 쓰는 신호를 같은 서버 계산에서 다시 만든다.
  - [x] 관제판 기본 query는 계속 가볍게 유지한다. 감사 이력, 전체 상세 편집 데이터, 정정 상세를 기본 관제판 row query에 넣지 않는다.
  - [x] anomaly threshold에서 값이 기준과 같은 경우는 초과가 아니다. 빈 재고/손실, 휴무 장부는 기존 Story 3.4의 명시 처리 규칙을 유지한다.

- [x] UX 검증과 오류 포커스 동작을 맞춘다. (AC: 1, 5, 6, 7)
  - [x] 저장 성공은 "저장됐습니다." 또는 기존 저장 성공 피드백 패턴으로 표시한다.
  - [x] 저장 실패는 한국어 오류와 재시도 가능한 상태를 제공한다.
  - [x] 필드 오류는 필드 하단 또는 행 안에 표시하고 첫 번째 오류 input/select로 포커스를 이동한다.
  - [x] 본사 상세에서 모달/Sheet를 중첩하지 않는다. 장부 상세 안에 탭/섹션/확장 패널을 두는 편이 안전하다.
  - [x] 모바일/좁은 폭에서도 상태 배지, 신호 칩, 저장 버튼, 오류 문구가 겹치지 않아야 한다.

- [x] 테스트를 추가하고 관련 회귀를 돌린다. (AC: 1-7)
  - [x] unit/source contract: 본사 수정 action이 `ledgerId`, 본사 권한, `IN_PROGRESS`/`IN_REVIEW` 허용, `HEADQUARTERS_CLOSED` 차단, audit before/after, revalidation을 포함하는지 검증한다.
  - [x] unit/source contract: 기존 store-entry today flow가 계속 `storeId` 기반으로 동작하고, 본사 ledger detail flow가 오늘 장부로 우회하지 않는지 검증한다.
  - [x] unit/source contract: audit action label에 본사 수정 구분이 포함되는지 검증한다.
  - [x] E2E: 본사가 관제판의 `검토대기` 행을 열어 매출/결제와 매입 또는 손실을 수정하고, 저장 후 상세/관제판 숫자와 이상 신호가 갱신되는지 확인한다.
  - [x] E2E: 본사 수정 audit log의 `actorId`, `targetType: "DailyLedger"`, `targetId`, `before`, `after`, `createdAt`을 확인한다.
  - [x] E2E: 지점장은 다른 지점 ledger detail에 접근할 수 없고, 본사마감 장부의 원본 수정 필드는 disabled 및 정정 안내를 표시한다.
  - [x] E2E: 한국어 검증 오류와 첫 오류 포커스 이동을 확인한다.
  - [x] 권장 검증 명령: `pnpm test:unit`, `pnpm typecheck`, `pnpm lint`, `pnpm test:e2e -- tests/e2e/hq-dashboard.spec.ts`, 새 4.1 대상 E2E, `git diff --check`.

### Review Findings

- [ ] [Review][Patch] 같은 상세 페이지에서 다른 탭 저장 후 손실 저장이 잘못 충돌 처리된다 [src/features/losses/hq-edit-actions.ts:225]
- [ ] [Review][Patch] 비용/매입/재고 등 destructive save 경로가 `updatedAt` 충돌 가드 없이 오래된 폼으로 최신 행을 덮어쓸 수 있다 [src/features/ledger/hq-edit-actions.ts:125]
- [ ] [Review][Patch] 재고 조정/손실 저장은 검증 실패를 반환해도 먼저 `DailyLedger.updatedById/updatedAt`을 변경하고 감사 로그 없이 커밋할 수 있다 [src/features/inventory/hq-edit-actions.ts:335]
- [ ] [Review][Patch] 본사 수정 감사 payload가 `workMemo`와 비용/매입 행 상세를 누락해 before/after만으로 실제 변경을 구분할 수 없다 [src/features/ledger/queries.ts:219]
- [ ] [Review][Patch] 본사 재고 저장이 표시용 carryover 행까지 실제 입력 행으로 저장한다 [src/features/inventory/hq-edit-actions.ts:170]
- [ ] [Review][Patch] 비활성/잘못된 매입 기준은 행 검증 오류가 아니라 일반 저장 실패로 떨어져 첫 오류 포커스를 맞출 수 없다 [src/features/ledger/hq-edit-actions.ts:386]
- [ ] [Review][Patch] 본사 비용 저장이 활성 `EXPENSE_ITEM` 코드인지 서버에서 확인하지 않아 다른 코드 그룹이나 비활성 코드가 저장될 수 있다 [src/features/ledger/hq-edit-actions.ts:261]
- [ ] [Review][Patch] 본사 상세 헤더가 마지막 수정자/수정 시각을 유지하지 않는다 [src/app/app/ledgers/[ledgerId]/page.tsx:115]

## Dev Notes

### Source Context

- Epic 4의 목표는 본사가 장부를 직접 보완하거나 수정하고, 최종 마감해 원본을 잠그며, 마감 후에는 원본을 보존한 채 정정 기록과 정정 반영값으로 운영 숫자를 바로잡는 것이다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\epics.md#Epic-4`]
- Story 4.1은 Epic 4의 첫 스토리이며, 마감 전 원본 보완/수정 가능 상태를 구현한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\epics.md#Story-4.1`]
- PRD FR-18은 본사 사용자가 모든 지점의 본사 마감 전 일일 장부를 직접 입력하거나 수정할 수 있고, 본사 수정이 변경 이력에 남으며, 수정 후 계산값과 이상 신호가 갱신되어야 한다고 정의한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\prds\prd-erp_fish-2026-05-28-2\prd.md#FR-18`]
- PRD 상태 모델은 `입력 중`과 `검토 대기`를 본사와 해당 지점장이 수정 가능한 상태로 두고, `본사 마감`만 원본 수정 불가 상태로 둔다. 지점장 제출 또는 검토 대기 전환은 원본 잠금이 아니다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\prds\prd-erp_fish-2026-05-28-2\prd.md#3.1-장부-상태-모델`]
- PRD review resolution도 같은 결정을 확정한다. 원본 장부가 잠기는 시점은 본사 마감이고, 본사 마감 후에는 정정 기록만 추가할 수 있다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\prds\prd-erp_fish-2026-05-28-2\review-resolution.md#1-지점장-제출-후-잠금-vs-본사-마감-잠금`]
- UX Flow 3은 본사 이스태프가 관제판에서 `검토대기` 장부를 열고 매입 누락을 추가한 뒤 저장, 계산값 재확인, 마감으로 이어지는 흐름을 기준으로 한다. Story 4.1은 이 중 마감 전 보완/수정까지만 담당한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Flow-3`]

### Current Repository State

- `src/app/app/ledgers/[ledgerId]/page.tsx`는 현재 본사 전용 read-only 상세 페이지다. `requireHeadquartersUser()`와 `getHqLedgerDetail(ledgerId)`를 사용하고, 상태/신호/주요 숫자 카드만 보여준다.
- `src/features/dashboard/queries.ts`의 `getHqLedgerDetail()`은 ledger id 기준으로 상세 데이터를 읽지만 편집 폼용 전체 장부 데이터는 반환하지 않는다.
- 기존 입력 UI는 `src/app/app/store-entry/page.tsx`와 `src/app/app/store-entry/inventory/page.tsx`, `src/app/app/store-entry/losses/page.tsx`에 있고, `storeId` query param을 받을 수 있다.
- 가장 큰 현재 제약: 기존 입력 query/action 대부분은 `ledgerId`가 아니라 `storeId`로 오늘 KST 장부를 찾거나 생성한다. `getTodayKstMidnight()`, `getOrCreateTodayStoreLedgerInTx()`, `getTodayStoreLedger()` 흐름을 본사 ledger detail에서 그대로 쓰면 선택한 장부가 아니라 오늘 장부를 수정할 수 있다.
- `DailyLedgerStatus`는 `IN_PROGRESS`, `IN_REVIEW`, `HEADQUARTERS_CLOSED`, `HOLIDAY`를 가진다. `DailyLedger`에는 `storeId`, `closingDate`, 매출/결제, 근무 정보, 제출자/제출시각, 하위 장부 항목 관계, `createdById`, `updatedById`가 있다.
- 현재 schema에는 `CorrectionRecord`, `closedBy`, `closedAt`이 없다. Story 4.1에서 정정 모델이나 본사 마감 필드를 선행으로 만들지 않는다.
- `requireStoreAccess(storeId)`는 본사 사용자에게 모든 지점 접근을 허용하고, 지점장은 활성 배정 지점만 허용한다. 본사 전용 상세 페이지는 `requireHeadquartersUser()`를 이미 사용한다.
- 매출/비용/매입/근무 저장 action은 `editableLedgerStatuses = ["IN_PROGRESS", "IN_REVIEW"]`로 `검토대기` 수정을 허용한다. 단, 오늘 장부 기준이므로 4.1에서 그대로 쓰면 안 되는 경로가 있다.
- 재고/손실 action과 UI는 `HEADQUARTERS_CLOSED` 차단과 한국어 안내 패턴을 이미 갖고 있다. 이 패턴을 본사 상세 편집에서도 재사용한다.

### Architecture Guardrails

- 스택은 Next.js App Router + Server Components/Server Actions + Prisma + PostgreSQL + Zod + shadcn/ui + Tailwind CSS를 유지한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Core-Architectural-Decisions`]
- 내부 mutation은 Server Actions로 구현한다. 공개 API route를 새로 만들 필요가 없다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#API-Design`]
- 모든 ledger, report, correction, master-data, settings action은 서버에서 authorization을 강제해야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Authorization-Strategy`]
- 업무 변경 action은 feature schema로 입력을 검증하고, 권한을 확인하고, transaction 안에서 비즈니스 변경과 감사 기록을 저장하고, 필요한 route를 revalidate하고, `ActionResult`를 반환한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Implementation-Patterns-Consistency-Rules`]
- 감사 이벤트 이름은 dot notation을 사용한다. 예: `ledger.updated`, `ledger.closed`, `correction.created`, `threshold.updated`. Story 4.1의 본사 수정 action도 같은 스타일을 따른다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Event-and-Audit-Patterns`]
- 관제판, 장부 상세, 리포트는 같은 서버 측 계산 함수를 공유해야 한다. UI 컴포넌트 안에 새 계산 공식을 만들면 안 된다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Anti-Patterns`]
- PRD 정책이 우선이다. `검토 대기`는 원본 잠금이 아니고, 원본 장부는 본사 마감 때만 잠긴다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Important-Handoff-Notes`]

### UX Guardrails

- 관제판 행은 장부 상세로 이동한다. 데스크탑은 Sheet 또는 상세 페이지, 모바일은 전체 페이지 이동을 허용한다. 현재 구현은 상세 페이지가 있으므로 이번 story에서 Sheet 전환을 강제하지 않는다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Screen-Map`]
- 상태 배지는 `미입력`, `입력중`, `검토대기`, `본사마감`, `휴무` 5종이며 텍스트로 의미를 전달해야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Component-Patterns`]
- `검토대기`는 `badge-review`이며 원본 잠금이 아니다. 본사와 지점장 모두 수정 가능하다. `본사마감`은 `badge-closed`이며 원본 입력 필드는 disabled이고 정정 기록 추가만 가능하다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#State-Interaction-Matrix`]
- 저장 성공 문구는 "저장됐습니다."를 우선 사용한다. 오류 문구는 한국어로 짧고 구체적이어야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Content-Voice`]
- 모달 스택은 1단계 이하이며, Dialog 위 Dialog 또는 Sheet 위 Sheet 중첩을 피한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Interaction-Principles`]
- 재고 테이블은 냉동/생물 탭, 품목, 규격, 전일재고, 매입, 당일재고, 재고금액, 수량 컬럼을 표시하고 수정된 행을 강조한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Component-Patterns`]
- 접근성: 상태 배지는 aria-label을 제공하고, 재고 테이블 헤더는 `scope="col"`을 쓰며, 수정된 행은 텍스트/aria로 의미가 전달되어야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Accessibility-Notes`]

### Files To Read Before Editing

- `src/app/app/ledgers/[ledgerId]/page.tsx`: 본사 상세 route. 현재 read-only 구조와 dashboard return query 보존을 확인한다.
- `src/features/dashboard/queries.ts`: `getHqLedgerDetail()`, dashboard row select, signal 계산, priority/filter/sort 흐름을 확인한다.
- `src/features/dashboard/components/hq-dashboard-table.tsx`: 상세 링크와 `date/sort/filter` 전달 계약을 확인한다.
- `src/features/ledger/queries.ts`: `getTodayKstMidnight()`, `getOrCreateTodayStoreLedgerInTx()`, `getTodayStoreLedger()`의 today-only 동작을 확인한다.
- `src/features/ledger/actions.ts`: 매출/비용/매입/근무 저장 action, `editableLedgerStatuses`, audit payload, revalidation을 확인한다.
- `src/features/ledger/schemas.ts`: 기존 Zod 검증과 한국어 field error 메시지를 재사용한다.
- `src/features/ledger/components/sales-payment-step-client.tsx`: 매출/결제 폼, 오류 포커스, 저장 피드백 패턴을 확인한다.
- `src/features/ledger/components/expense-step-client.tsx`: 비용 행 폼과 첫 오류 포커스 패턴을 확인한다.
- `src/features/ledger/components/purchase-step-client.tsx`: 매입 기준 선택, 기존 snapshot purchase 처리, 행 오류 포커스 패턴을 확인한다.
- `src/features/ledger/components/workstep-client.tsx`: 근무 정보 저장과 memo validation 패턴을 확인한다.
- `src/features/inventory/queries.ts`: display-only carryover와 saved inventory rows를 구분하는 현재 규칙을 확인한다.
- `src/features/inventory/actions.ts`: 재고 저장, 재고 조정, closed-blocking, `reconcileLedgerInventoryAdjustments` 흐름을 확인한다.
- `src/features/inventory/components/inventory-step-client.tsx`: 냉동/생물 탭, 조정 사유, closed UI, 오류 포커스 패턴을 확인한다.
- `src/features/losses/actions.ts`: 손실 저장, stale ledger guard, closed-blocking, audit payload를 확인한다.
- `src/features/losses/components/loss-step-client.tsx`: 손실 행 폼, 오류 포커스, closed UI를 확인한다.
- `src/features/audit/audit-format.ts`: `DailyLedger` label과 action label map에 본사 수정 label을 추가한다.
- `src/features/audit/audit-queries.ts`: `DailyLedger` 대상 이름 해석과 actor 표시 패턴을 확인한다.
- `tests/unit/ledger-submit.test.mjs`, `tests/unit/ledger-inventory.test.mjs`, `tests/unit/ledger-losses.test.mjs`, `tests/unit/hq-dashboard.test.mjs`: source contract 패턴을 따른다.
- `tests/e2e/hq-dashboard.spec.ts`, `tests/e2e/store-ledger-review.spec.ts`: 본사/지점장 fixture, cleanup, dashboard navigation, no-overlap 패턴을 재사용한다.

### Data and Domain Rules

- 수정 가능 상태는 `IN_PROGRESS`와 `IN_REVIEW`다.
- 원본 수정 금지 상태는 `HEADQUARTERS_CLOSED`와 `HOLIDAY`다. `HOLIDAY` 처리 정책이 애매하면 원본 수정 불가로 둔다.
- `IN_REVIEW` 저장 후 상태를 `IN_PROGRESS`로 되돌리면 안 된다.
- `submittedById`와 `submittedAt`은 본사 보완 저장으로 바꾸면 안 된다.
- `updatedById`는 마지막 수정자 표시와 관제판 최종 수정자에 쓰이므로 본사 저장 시 본사 사용자로 갱신한다.
- 기존 계산은 integer KRW와 integer quantity를 사용한다. 문자열 포맷은 UI에서만 한다.
- `paymentDifference`는 PRD의 매출차액과 다르다는 Story 3.3 판단을 유지한다.
- dashboard 기본 row query는 가볍게 유지한다. 상세 편집 데이터는 ledger detail query에서 읽는다.

### Previous Story Intelligence

- Story 2.8에서 `IN_REVIEW`는 원본 잠금이 아니며 기존 save actions가 `IN_REVIEW`를 허용하도록 정리됐다. Story 4.1은 이 정책을 본사 상세 편집에서도 지켜야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\implementation-artifacts\2-8-입력자가-장부를-검토-대기로-제출한다.md`]
- Story 2.8에서 제출 metadata와 `ledger.review.submitted` audit log가 추가됐다. 본사 보완 저장은 제출 metadata를 바꾸지 말고 별도 본사 수정 audit action을 남겨야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\implementation-artifacts\2-8-입력자가-장부를-검토-대기로-제출한다.md`]
- Story 3.5에서 dashboard `date/sort/filter` query state와 상세 복귀 링크가 구현됐다. Story 4.1은 편집 후에도 이 상태를 보존해야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\implementation-artifacts\3-5-본사가-회의-전에-문제-지점을-빠르게-추적한다.md`]
- Story 3.4에서 dashboard와 ledger detail이 같은 anomaly helper로 재고/손실 critical signal을 계산한다. Story 4.1 저장 후에는 이 신호가 같은 shared helper로 갱신되어야 한다. [Source: `C:\Code\Project\erp_fish\_bmad-output\implementation-artifacts\3-4-본사가-재고-손실-이상-신호를-본다.md`]
- Story 3.1/3.5는 관제판 기본 query가 무거운 상세 데이터를 가져오지 않도록 했다. 4.1 편집 데이터 때문에 dashboard query를 비대하게 만들지 않는다.
- Story 2.7 review에서 표시용 재고 행을 실제 입력처럼 계산하는 문제가 잡혔다. 본사 재고 보완 UI에서도 display-only carryover와 saved rows를 분리한다.
- Story 2.5/2.6 흐름에서 `quantity`, `currentQuantity`, adjustment 의미가 나뉘어 있다. 본사 수정이 이 의미를 합치면 안 된다.
- 최근 커밋 `b1fc916 feat: add HQ anomaly signal workflows`는 anomaly settings, anomaly helper, HQ ledger detail, dashboard signal wiring을 추가했다. 4.1은 이 결과를 소비한다.
- 최근 커밋 `0b43835 feat: add ledger submission and HQ dashboard`는 `submittedById/submittedAt`, `submitLedgerForReview`, dashboard table, `IN_REVIEW` edit policy를 추가했다.
- 최근 커밋 `f024a7d Complete ledger loss entry flow`는 손실/재고 수정 회귀 위험과 직접 연결된다.

### Implementation Recommendations

- 가장 안전한 순서:
  1. `ledgerId` 기준 read query와 본사 권한 guard를 만든다.
  2. `ledgerId` 기준 save action을 매출/결제부터 하나 추가하고 RED test로 오늘 장부 오수정 위험을 막는다.
  3. 비용/매입/근무, 재고/조정, 손실로 같은 패턴을 확장한다.
  4. `src/app/app/ledgers/[ledgerId]/page.tsx`에 탭 또는 섹션별 편집 UI를 붙인다.
  5. audit label과 E2E를 연결한다.
- 기존 store-entry 컴포넌트를 재사용할 경우 props로 action target을 분리한다. 예: `mode: "today-store-entry" | "hq-ledger-detail"` 또는 action prop injection. 문자열 mode가 많아지면 작은 wrapper component를 둔다.
- 기존 actions를 하나의 범용 action으로 바꿀 경우 지점장 today flow 회귀 위험이 크다. 먼저 작은 ledgerId 본사 action을 별도로 두고, 중복이 명확해진 후 공통 transaction helper를 뽑는 편이 안전하다.
- `getHqLedgerDetail()`에 편집용 하위 rows를 모두 넣으면 read-only detail과 dashboard detail 책임이 커질 수 있다. 필요하면 `src/features/ledger/hq-detail-queries.ts` 같은 ledger feature query를 두고 page에서 조합한다.
- Story 4.1은 `CorrectionRecord` 구현이 아니다. closed ledger에서는 정정 안내만 하고, 정정 생성은 Story 4.3로 남긴다.

### Latest Technical Information

- 현재 `package.json` 기준 로컬 스택은 Next.js `^15.2.3`, React `^19.0.0`, Prisma `^6.6.0`, Playwright `^1.60.0`, shadcn `^4.8.2`다. Story 4.1에서 major upgrade를 하지 않는다. [Source: `C:\Code\Project\erp_fish\package.json`]
- 2026-06-01 `pnpm view` 기준 최신 stable은 Next.js `16.2.6`, Prisma Client `7.8.0`, React `19.2.6`, Playwright `1.60.0`이다. 이 차이는 story 구현 중 업그레이드 요구가 아니며, 현재 lockfile과 앱 스택을 유지한다.
- Next.js 공식 문서 기준 `revalidatePath`는 Server Functions와 Route Handlers에서 호출할 수 있고, 이 story의 Server Action 저장 후 상세/관제판 갱신에 적합하다. [External: `https://nextjs.org/docs/app/api-reference/functions/revalidatePath`]
- Next.js App Router는 navigation 후 page가 갱신된 `searchParams` prop을 받는다. `date/sort/filter` 보존은 Server Component `searchParams`와 Link href로 유지한다. [External: `https://nextjs.org/docs/app/api-reference/functions/use-search-params`]
- Prisma 공식 문서 기준 `select`는 필요한 필드만 가져와 응답 크기와 query 비용을 줄일 수 있다. dashboard 기본 query를 비대하게 만들지 말고 상세 편집 query와 분리한다. [External: `https://docs.prisma.io/docs/v6/orm/prisma-client/queries/select-fields`]
- Prisma interactive transaction은 Prisma query와 사용자 코드를 하나의 transaction 함수 안에서 실행할 수 있다. ledger update와 audit insert는 짧은 transaction 안에서 처리한다. [External: `https://www.prisma.io/docs/orm/v6/prisma-client/queries/transactions`]
- Playwright keyboard API는 Enter/Space/Tab 입력 검증을 지원한다. 본사 상세의 키보드 접근과 첫 오류 포커스 검증에 사용한다. [External: `https://playwright.dev/docs/api/class-keyboard`]

### Testing Requirements

- 새 unit/source contract 파일 이름 후보: `tests/unit/hq-ledger-edit.test.mjs`.
- source contract는 본사 ledger edit action이 `ledgerId`를 입력으로 받고, `getTodayStoreLedger`나 `getTodayKstMidnight`를 통해 저장 대상을 고르지 않는지 확인한다.
- source contract는 `requireHeadquartersUser()` 또는 동등한 본사 권한 확인, `status in ["IN_PROGRESS", "IN_REVIEW"]`, `HEADQUARTERS_CLOSED` 차단, `writeAuditLog`, 본사 audit action label, `revalidatePath("/app/ledgers/")` 또는 상세 경로 갱신을 확인한다.
- E2E fixture는 오늘/어제 KST 날짜를 helper로 만든다. 고정 월 문자열을 쓰지 않는다.
- E2E cleanup은 audit logs, ledger child rows, daily ledger, store assignments, store, story-specific users/products/codes를 FK 순서대로 정리한다.
- 본사 E2E는 가능하면 한 테스트에서 모든 섹션을 무리하게 다루지 말고, 핵심 2개 섹션 plus source contract로 전체 action coverage를 보완한다.
- 전체 회귀 후보:
  - `pnpm test:unit`
  - `pnpm typecheck`
  - `pnpm lint`
  - `pnpm test:e2e -- tests/e2e/hq-dashboard.spec.ts`
  - `pnpm test:e2e -- tests/e2e/store-ledger-review.spec.ts`
  - 새 Story 4.1 E2E
  - `git diff --check`

### Open Questions To Preserve

- 본사 상세를 최종 UX처럼 Sheet로 만들지, 현재 상세 페이지를 확장할지는 제품 결정이 남아 있다. 현재 코드에는 상세 페이지가 있으므로 이번 story는 페이지 확장으로도 AC를 만족할 수 있다.
- 본사 수정과 지점장 수정을 audit에서 구분하는 방식은 action label로 충분한지, actor role snapshot이 필요한지는 추후 운영 요구에 따라 바뀔 수 있다. 이번 story는 action label과 actor만으로 시작한다.
- 비용/매입 저장의 delete/create 패턴이 행별 `createdBy/createdAt` 보존 요구와 충돌하는지는 구현 중 확인해야 한다. 충돌하면 해당 섹션은 upsert/delete 분리로 조정한다.
- closed ledger에서 정정 안내 버튼을 보여줄 수는 있지만, 실제 정정 기록 생성은 Story 4.3 범위다.

### Project Context Reference

- `project-context.md`는 프로젝트 루트 하위에서 발견되지 않았다.
- 기준 컨텍스트는 BMad planning artifacts, PRD, architecture, UX 문서, Story 2.8/3.4/3.5 결과 파일, 현재 codebase, read-only GPT-5.5 high-reasoning subagent 분석 결과다.

### Completion Note

- Ultimate context engine analysis completed - comprehensive developer guide created.

### References

- [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\epics.md#Story-4.1`]
- [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\prds\prd-erp_fish-2026-05-28-2\prd.md#FR-18`]
- [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Implementation-Patterns-Consistency-Rules`]
- [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\architecture.md#Important-Handoff-Notes`]
- [Source: `C:\Code\Project\erp_fish\_bmad-output\planning-artifacts\ux-designs\ux-erp_fish-2026-05-28\EXPERIENCE.md#Flow-3`]
- [Source: `C:\Code\Project\erp_fish\src\app\app\ledgers\[ledgerId]\page.tsx`]
- [Source: `C:\Code\Project\erp_fish\src\features\ledger\queries.ts`]
- [Source: `C:\Code\Project\erp_fish\src\features\ledger\actions.ts`]
- [Source: `C:\Code\Project\erp_fish\src\features\inventory\actions.ts`]
- [Source: `C:\Code\Project\erp_fish\src\features\losses\actions.ts`]

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- Story context created by `bmad-create-story` workflow.
- 2026-06-01: Loaded BMM config, sprint status, create-story template/checklist, BMad customization, planning artifact discovery protocol, package versions, and git status.
- 2026-06-01: `python3` customization resolver returned the Windows Python launcher stub output, so workflow customization was resolved with `python`.
- 2026-06-01: No `project-context.md` file was found under the project root during persistent-facts discovery.
- 2026-06-01: Auto-selected the first backlog story from sprint status: `4-1-본사가-검토-대기-장부를-보완하고-수정한다`.
- 2026-06-01: Used read-only GPT-5.5 high-reasoning subagents for planning-artifact analysis, codebase guardrail analysis, previous story intelligence, and git history.
- 2026-06-01: Loaded Story 4.1/Epic 4 extracts, PRD FR-18/FR-21 context, UX Flow 3, architecture handoff notes, current ledger/dashboard/inventory/loss/audit files, previous Story 2.8/3.4/3.5 files, and recent git commit stats.
- 2026-06-01: Checked latest package data with `pnpm view` and official Next.js, Prisma, and Playwright docs for implementation-specific APIs.
- 2026-06-01: Started `bmad-dev-story` implementation and moved sprint tracking to in-progress.
- 2026-06-01: Added RED source-contract tests for HQ ledger edit boundaries, actions, client action injection, detail page wiring, and audit labels.
- 2026-06-01: Added ledgerId-based HQ query/action paths for ledger, inventory, and losses without using today-ledger creation flows.
- 2026-06-01: Extended existing store-entry client components to accept injected HQ save actions and include `ledgerId` while preserving the default store-entry flow.
- 2026-06-01: Reworked the HQ ledger detail page into editable tabs for sales/payment, expenses, purchases, inventory, losses, and work information.
- 2026-06-01: Debugged E2E fast-input timing by switching test entry to keyboard-like replacement and reading current input refs for save payloads.
- 2026-06-01: Verified with `pnpm test:unit`, `pnpm typecheck`, `pnpm lint`, `pnpm test:e2e tests/e2e/hq-ledger-edit.spec.ts`, `pnpm test:e2e tests/e2e/hq-dashboard.spec.ts`, and `git diff --check`.

### Completion Notes List

- HQ users can now edit `IN_PROGRESS` and `IN_REVIEW` ledgers directly from `/app/ledgers/[ledgerId]` across sales/payment, expenses, purchases, inventory, inventory adjustments, losses, and work information.
- HQ save actions use `ledgerId`, require headquarters authorization, update `updatedById`, preserve submission metadata, block closed/holiday original edits, write role-distinct audit actions, and revalidate detail/dashboard/store-entry paths.
- Existing store-entry client components remain reusable with default store-manager actions while HQ detail injects ledgerId-based actions.
- Closed ledgers show original-edit disabled controls and correction guidance; correction creation remains Story 4.3 scope.
- E2E coverage now exercises all editable sections, audit payload presence, closed-ledger disablement, and Korean validation error focus.

### File List

- `_bmad-output/implementation-artifacts/4-1-본사가-검토-대기-장부를-보완하고-수정한다.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `src/app/app/ledgers/[ledgerId]/page.tsx`
- `src/features/audit/audit-format.ts`
- `src/features/inventory/components/inventory-step-client.tsx`
- `src/features/inventory/hq-edit-actions.ts`
- `src/features/inventory/queries.ts`
- `src/features/ledger/components/expense-step-client.tsx`
- `src/features/ledger/components/purchase-step-client.tsx`
- `src/features/ledger/components/sales-payment-step-client.tsx`
- `src/features/ledger/components/workstep-client.tsx`
- `src/features/ledger/hq-edit-actions.ts`
- `src/features/ledger/queries.ts`
- `src/features/losses/components/loss-step-client.tsx`
- `src/features/losses/hq-edit-actions.ts`
- `src/features/losses/queries.ts`
- `tests/e2e/hq-ledger-edit.spec.ts`
- `tests/unit/hq-ledger-edit.test.mjs`

### Change Log

- 2026-06-01: Story 4.1 문서 생성(ready-for-dev)
- 2026-06-01: Story 4.1 개발 시작(in-progress)
- 2026-06-01: ledgerId 기반 본사 장부 상세 편집 구현 완료(review)
