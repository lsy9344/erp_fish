# 2026-09-08 배포 전 검토 실행 기록

## 결정

- 코드 배포 판정: **GO**
- 운영 환경에서는 `ENABLE_LABOR_EXPORT`를 설정하지 않는다.
- CAP-9 인건비 내보내기 정책이 서면 승인되기 전까지 인건비 Excel은 화면과 API에서 모두 닫아 둔다.
- 2026년 8월의 미완료 장부는 화면에서 미입력·진행 중으로 구분되므로 코드 배포를 막지 않는다. 임의로 영업 데이터를 채우지 않는다.
- 이번 변경에는 데이터베이스 구조 변경이나 마이그레이션이 없다.

## 배포 전 보완

- 인건비 Excel을 기본 비활성 기능으로 바꾸고, 서버 API도 같은 환경 설정으로 차단했다.
- 새 직급별 직원 선택 화면을 따라가지 못한 오래된 E2E 검사 3곳을 수정했다.
- 직원 연결 복구 중 부수적으로 바뀐 운영 인건비를 원본 감사 값인 0원으로 복원했다.
  - 근무 기록: `cmth317pt00dijq04ns8c7f1k`
  - 직원 연결: `cmtc5oj1z000pl404l54ilzks` 유지
  - 원복 감사 로그: `cmtsino3b0001m72016uwscez`
- 2026-09-08 20:18 KST 읽기 전용 재확인에서 직원 연결 유지, 금액 0원, 원복 감사 로그를 확인했다.

## 검증 결과

- 데이터베이스 설정 검사, 문서·코드 형식 검사, 타입 검사, lint, production build 통과
- 단위 테스트: 748개 통과
- API 테스트: 30개 통과
- 핵심 E2E:
  - 장부: 76개 통과
  - 본사: 오래된 검사 수정 후 전체 94개 통과
  - 관리자·권한: 29개 통과
  - 이카운트 가져오기: 10개 통과
- 새 화면으로 고친 회의 검증 1개 별도 통과
- `git diff --check` 통과
- `pnpm audit --audit-level high` 통과: 높음 0개, 낮음 2개, 보통 5개
- 검사 종료 후 3102 포트에 남은 개발 서버 없음

## 실제 배포 직전 운영 확인

- 2026-09-09 KST 운영 DB 새 백업 생성 및 복원 목록 확인 완료
  - 파일: `/home/noah/.local/share/erp-fish/backups/erp-fish-predeploy-2026-09-08.dump`
  - 크기: 39,709,625 bytes, 권한: `600`
- Vercel production의 `AUTH_SECRET`, `DATABASE_URL`, `DATABASE_URL_UNPOOLED` 존재 확인 완료. 값은 출력하거나 저장소에 남기지 않았고 임시 env 파일도 삭제했다.
- 운영 권한 읽기 전용 확인 완료
  - 활성 지점장 10명 중 `LEDGER_EDIT` 누락 0명
  - 활성 본사 4명 중 `EXPORT_CREATE` 보유 4명
- 전체 `pnpm release:preflight` 단일 실행 통과
  - 단위 748개, API 30개, 장부 76개, 본사 94개, 관리자·권한 29개, 이카운트 10개
- `pnpm audit --audit-level high` 통과: 높음 0개, 낮음 2개, 보통 5개
- 냉동 전환 버튼은 생물 품목에만 보이는 비활성 `type="button"`이며 저장 함수와 연결하지 않았다. E2E에서 강제 클릭 전후 생물·냉동 재고 수량과 금액이 같음을 확인했다.
- 냉동 전환용 DB 구조, 저장 action, FIFO 계산 변경은 이번 배포에 없다.
- 별칭 저장 중 코드 종류가 바뀌는 권한 경합은 트랜잭션 행 잠금과 재확인으로 차단했다.
- 기존 포트폴리오·계획서 변경과 별도 금액 복원 스크립트는 배포 커밋에서 제외한다.
- 푸시 후 GitHub CI와 Vercel production 배포 URL을 최종 기록한다.
