# 2026-09-08 0905 직원 연결 복구 실행 기록

## 대상

- 지점: 안양참수산
- 영업일: 2026-08-31
- 근무 기록: `cmth317pt00dijq04ns8c7f1k`
- 직원: 김상우 (`cmtc5oj1z000pl404l54ilzks`)

## 확인 및 적용

- 사전 확인: 같은 영업일·지점에 김상우 근무 기록이 중복되지 않음을 확인했다.
- `--dry-run`: 직원 연결 대상과 본사 권한·지점 범위를 확인했다.
- `--apply`: `employeeId`를 연결하고 감사 로그를 남겼다.
- 감사 로그: `cmts9wdhp0001m78x3cj6urur`
- 적용 후 확인: 해당 직원의 2026년 8월 근무 기록 21건(안양참수산 20건, 구로 1건)을 조회했다.

## 금액 보호 개선

최초 적용 당시 대상 행의 금액은 0원에서 직원 일급으로 바뀌었다. 이후 복구 스크립트는 금액을 건드리지 않도록 수정했다. 앞으로 `--apply`는 직원 연결 정보와 감사 정보만 바꾸며, 금액은 기존 값을 유지한다.

## 배포 전 재검토 및 금액 원복

- 읽기 전용 재확인 결과, 대상 행의 현재 금액은 `180,000원`이다.
- 감사 로그 `cmts9wdhp0001m78x3cj6urur`에는 금액이 `0원`에서 `180,000원`으로 바뀐 사실이 남아 있다.
- 스크립트 수정은 다음 실행부터 금액 변경을 막을 뿐, 이미 바뀐 운영 데이터는 되돌리지 않는다.
- 복구 목적이 `employeeId` 연결로 한정되고 원본 감사 값도 `0원`이므로, 배포 검토에서 부수 금액 변경을 원복하기로 결정했다.
- 2026-09-08 19:18 KST에 `scripts/restore-labor-amount-from-audit.mjs`로 원본 감사 기록과 현재 값을 대조한 뒤 `180,000원`을 `0원`으로 복원했다.
- 직원 연결 `employeeId=cmtc5oj1z000pl404l54ilzks`는 그대로 유지했다.
- 원복 감사 로그: `cmtsino3b0001m72016uwscez` (`ledger.labor_amount.restored_from_audit`)
- 적용 후 읽기 전용 재확인에서 `employeeId` 유지, 금액 `0원`, 새 감사 로그의 전후 값과 사유를 확인했다.
