# rev 2·3 운영 데이터 및 회귀 검증 기록

## 운영 DB 읽기 전용 확인

- 실행 시각: 2026-07-18 00:35:34 KST
- 연결 대상: `.env.local`의 `DATABASE_URL_UNPOOLED`
- 대상 확인 근거: 저장소 인수인계 문서가 이 변수를 Neon 운영 DB direct 연결로 명시한다.
- 식별 정보: host `ep-falling-…neon.tech` (일부 마스킹), database `neondb`
- 실행 방법: 트랜잭션 시작 직후 `SET TRANSACTION READ ONLY`를 적용하고 작업지시서의 `SELECT`와 직전 장부 조회만 실행했다.
- 비밀번호, 전체 연결 URL, 고객 계정 정보는 출력하거나 기록하지 않았다.

### 조회 결과

| 항목                          | 결과                                                                          |
| ----------------------------- | ----------------------------------------------------------------------------- |
| 지점                          | `cmqs3iy9a0009jrdwxbkcxmy4` / 제일수산                                        |
| 대상 영업일                   | 2026-07-16                                                                    |
| 대상 장부                     | `cmro9kypl0000jm04wgpw7ew8` (`closingDate` 원시값 `2026-07-17T00:00:00.000Z`) |
| 품목                          | `cmqvm01dm0019l1049jobamhk` / 참소라 / 생물 / 중 / 활성                       |
| 같은 이름·규격의 활성 품목 수 | 1개 (중복 활성 ID 0개)                                                        |
| 당일 매입                     | product ID `cmqvm01dm0019l1049jobamhk`, 수량 1                                |
| 당일 저장 재고                | product ID `cmqvm01dm0019l1049jobamhk`, `currentQuantity` 0, `quantity` 0     |
| 당일 이월 상태                | `carryoverSource` `MANUAL`, `carryoverStatus` `DATA_INSUFFICIENT`             |
| 직전 장부                     | `cmrmrctc3000ikv04u9x126zp` (`closingDate` 원시값 `2026-07-16T00:00:00.000Z`) |
| 직전 장부의 같은 품목 재고    | 행 없음 (`productId`, `currentQuantity`, `quantity` 모두 `null`)              |
| 2026-07 월초 스냅샷           | 행 없음                                                                       |

### 판정

동일 productId의 당일 매입이 재고 근거를 제공하므로 현재 손실 저장 검증을 우회하지 않는다.

고객 데이터 수정 없음. `INSERT`, `UPDATE`, `DELETE`, DDL, migration을 실행하지 않았다.

## 회귀 검증 결과

| 검증                                 | 결과          |
| ------------------------------------ | ------------- |
| `pnpm typecheck`                     | 통과 (exit 0) |
| `pnpm lint`                          | 통과 (exit 0) |
| `pnpm test:unit`                     | 567/567 통과  |
| 변경 파일 Prettier 검사              | 통과          |
| `git diff --check`                   | 통과          |
| 지점 장부 E2E 최초 실행              | 55/59 통과    |
| 실패 격리: inventory `--grep=재확인` | 1/1 통과      |
| 실패 격리: sales `--grep=재방문`     | 3/3 통과      |
| 지점 장부 E2E 후속 전체 실행         | 56/59 통과    |
| 지점 장부 E2E 파일별 clean 재실행    | 59/59 통과    |
| 본사 dashboard + reports E2E         | 53/53 통과    |

최초 지점 묶음의 실패 4개 중 검토 문구 2개는 기대값을 새 문구로 맞춘 커밋 `2e54816` 반영 후 통과했다. 나머지 로그인 잔류와 fixture 장부 미발견은 clean 격리 실행에서 모두 통과했다.

후속 전체 실행에서는 앞서 실패한 테스트가 통과했지만 다른 3개가 로그인 실패 또는 품목 선택 UI 대기로 실패했다. 실패 위치가 실행마다 바뀌어 장시간 전체 묶음의 인증·fixture 경쟁으로 분류했다. 이후 6개 spec을 각각 초기화된 DB와 서버에서 실행해 59/59 통과를 확인했다.

운영 데이터와 생산 코드는 수정하지 않았다. 회귀 검증 과정에서 `store-ledger-review.spec.ts`는 경고 문구 3곳을 현재 문구로 맞췄고, `5a8da87`에서 이미 적용된 "부족 방향은 정상 판매이므로 재고 신호를 만들지 않음" 정책에 맞춰 남아 있던 `판매 추정 확인` 기대값을 비노출 검증으로 정정했다. 현재 결제 차액 지표 이름의 비노출 검증도 보강했다. 이 밖의 테스트 변경은 `ledger-purchase.test.mjs`, `ledger-validation.test.mjs`의 Prettier 포맷뿐이다.
