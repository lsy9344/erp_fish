# 전체 셀 반영 검증표 (WO-17)

- 작성일: 2026-06-28
- 원본 장부: `docs/reference_from_customer/장부-202605현대 (1).xlsx`, 시트 `26`
- 목적: 고객 장부의 주요 셀이 앱 화면/계산에 어떻게 반영되는지 표로 확인한다. 이 문서는 **새 계산을 구현하는 작업이 아니라**, WO-14(본사 홈 이중 매출/마진) 착수 전에 누락과 차이를 드러내는 검증 산출물이다.
- 선행 관계: WO-14는 이 표에서 "본사 홈 반영 범위"가 정리된 뒤 착수한다.

## 0. 읽는 법

- `반영 상태` 값
  - `확인됨`: 셀 의미·수식이 원본에서 확인됐고 앱 계산 대응이 명확하다.
  - `라벨 확인 필요`: 계산값은 앱에 있으나 화면 라벨이 장부 용어와 다를 수 있다. WO-14에서 라벨을 맞춘다.
  - `앱 확인 필요`: 본사 아이디로 `홈`/리포트 화면을 직접 열어 현재 표시값과 위치를 확인해야 한다. (2026-06-30 기준 모든 행이 `확인됨`/`제외`로 정리되어 이 상태가 남은 행은 없다.)
  - `의도적 제외`: 앱에 반영하지 않기로 한 셀. 제외 사유를 함께 남긴다.
- 마진율 반전 표시는 2026-06-30 지시로 삭제됐다. 본사 홈은 장부 기준과 분석 기준의 원값 및 차이 표시만 다룬다.

## 1. 확인 완료 셀 (기준값 고정)

| 기준 셀 | 의미 | 수식(원본 `26` 시트) | 원본 값 | 앱 대응 계산 | 현재 화면 위치 | 반영 상태 | 조치 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `AE4` | 분석 매출 | `AI36+AI63+AI76` | `3,514,811` | 판매가 계획(plannedUnitPrice) 기준 추정 매출 | 본사 홈 / 리포트 | 라벨 확인 필요 | 화면 라벨 `분석 매출`로 표시 (WO-14) |
| `AE5` | 분석 이익률 | `(AE4-(AG36+AG63+AG76))/AE4` ≈ `28.84%` | 약 `28.84%` | 분석 매출 - 분석 원가 기준 이익률 | 본사 홈 / 리포트 | 라벨 확인 필요 | 화면 라벨 `분석 이익률`로 표시 (WO-14) |
| `C5` | 장부 매출 | `C22+C23+C24+C36` | `3,491,000` | 장부 총매출(결제수단 합) | 본사 홈 / 장부 상세 | 라벨 확인 필요 | 화면 라벨 `장부 매출`로 표시 (WO-14) |
| `C17` | 장부 이익률 | `C15/C5` ≈ `28.35%` | 약 `28.35%` | 장부 매출 기준 이익률 | 본사 홈 / 장부 상세 | 라벨 확인 필요 | 화면 라벨 `장부 이익률`로 표시 (WO-14) |

주의:

- `AE4`, `AE5`, `C5`, `C17`의 의미는 확인됐다. `AE4`를 다시 의미 미확정 항목으로 되돌리지 않는다.
- 마진율 반전 표시를 새 요구로 되살리지 않는다.

## 2. WO-14 착수 전 채워야 할 셀 (구현자 작업분)

원본 `26` 시트의 값/수식을 ExcelJS로 추출해 채웠다(2026-06-30). 각 셀은 본사 홈에서 직접 노출되는 셀이 아니라 `AE4/AE5/C5/C17`(이미 1절에서 확인된 본사 홈 4개 값)을 만드는 **상위 계산의 원천 셀**이라, `반영 상태`는 그 상위 값이 화면에 맞게 나오는지로 판단한다.

| 기준 셀 | 의미 | 수식(원본 `26` 시트) | 원본 값 | 원천 데이터 | 상위 노출 값 | 반영 상태 | 조치 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `C15` | 장부 매출이익 | `=C5-C13` | `989,830` | 장부 매출 - 장부 원가 | 장부 이익률(`C17=C15/C5`, 본사 홈) | 제외 | 고객 화면 직접 노출값이 아니라 상위 계산의 원천 셀로만 사용. 장부 이익률로 확인됨(1절) |
| `AG36/AG63/AG76` | 분석 원가(품목군별) | `=SUM(AG8:AG35)`<br>`=SUM(AG39:AG62)`<br>`=SUM(AG66:AG75)` | `775,770`<br>`788,750`<br>`936,650` | FIFO/추정 원가 | 분석 이익률(`AE5`, 본사 홈), 품목군 합계(`AG36+AG63+AG76=2,501,170`) | 제외 | 고객 화면 직접 노출값이 아니라 상위 계산의 원천 셀로만 사용. 현행 앱은 기간 집계값으로 대체 표시 |
| `AI36/AI63/AI76` | 분석 매출(품목군별) | `=SUM(AI8:AI35)`<br>`=SUM(AI39:AI62)`<br>`=SUM(AI66:AI75)` | `1,078,460`<br>`1,131,120`<br>`1,305,231` | 판매가 계획 기준 추정 매출 | 분석 매출(`AE4=AI36+AI63+AI76=3,514,811`, 본사 홈) | 확인됨 | 합계가 1절 `AE4` 분석 매출과 일치. 현행 앱은 기간 집계값으로 대체 표시 |
| `C22/C23/C24` | 결제수단별 매출 | (입력값, 수식 없음) | `1,270,000`<br>`2,151,000`<br>`(빈칸)` | 현금/카드/기타 결제 | 장부 매출(`C5=C22+C23+C24+C36`, 본사 홈/장부 상세) | 확인됨 | 합산이 장부 매출에 반영됨. `C24`는 원본도 빈칸. 현행 앱은 기간 집계값으로 대체 표시 |
| `C36` | 기타 매출 | `=SUM(C28:C35)` | `70,000` | 장부 매출 보조 항목 | 장부 매출(`C5`, 본사 홈/장부 상세) | 확인됨 | 합산이 장부 매출에 반영됨. 현행 앱은 기간 집계값으로 대체 표시 |

교차 검증(원본 값으로 1절 본사 홈 4개 값이 재현됨):

- `AE4` 분석 매출 = `AI36+AI63+AI76` = `1,078,460+1,131,120+1,305,231` = `3,514,811` ✓
- `AE5` 분석 이익률 = `(AE4-(AG36+AG63+AG76))/AE4` = `(3,514,811-2,501,170)/3,514,811` ≈ `28.84%` ✓
- `C5` 장부 매출 = `C22+C23+C24+C36` = `1,270,000+2,151,000+0+70,000` = `3,491,000` ✓
- `C17` 장부 이익률 = `C15/C5` = `989,830/3,491,000` ≈ `28.35%` ✓

## 3. 의도적 제외 셀

| 기준 셀 | 사유 |
| --- | --- |
| 진수산 재고이상 관련 셀 | 의뢰자가 직접 분석하기로 했다(C-15). 구현 범위 제외. |
| POS 품목별 실제 판매단가 셀 | 현 시스템에 POS 품목별 실판매 데이터가 없다. 품목별 값은 항상 `추정`으로만 표시한다. |

## 4. 본사 홈 vs 월별/리포트 구분

- 본사 홈에 필요한 셀: `C5/C17`(장부 기준), `AE4/AE5`(분석 기준). WO-14에서 두 기준을 별도 필드로 나란히 표시하고, 두 매출 기준의 차이 금액과 차이율을 본사 전용으로 표시한다.
- 월별/리포트에 필요한 셀: 품목군별 매출/원가(`AI*`, `AG*`), 결제수단별 매출(`C22~C24`, `C36`). WO-15 월별 손익/품목매출 시트와 연결한다.

## 5. 검증

```bash
pnpm test:unit:file tests/unit/ledger-review.test.mjs tests/unit/hq-dashboard.test.mjs
```
