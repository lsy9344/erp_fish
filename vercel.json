{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "DATABASE_URL=\"${DATABASE_URL_UNPOOLED:-$DATABASE_URL}\" pnpm db:migrate && pnpm run build",
  "crons": [
    {
      "path": "/api/internal/notifications/morning-summary",
      "schedule": "0 23 * * *"
    }
  ]
}
